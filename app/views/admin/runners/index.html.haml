= content_for :title do
  %h3.project-title
    Manage Runners

    .pull-right
      %small
        = pluralize(@runners.total_count, 'runner')

%p.lead
  %span To register new runner you should use next token
  %code #{GitlabCi::RUNNERS_TOKEN}

.bs-callout
  %p
    A 'runner' is a process which runs a build.
    You can setup as many runners as you need.
    %br
    Runners can be placed on separate users, servers,  even on your local machine
    %br

  %div
    %span Each runner can be in one of next states:
    %ul
      %li
        %span.label.label-success shared
        \- run any builds
      %li
        %span.label.label-info specific
        \- run only builds from assigned projects



%table.table
  %tr
    %th ID
    %th Type
    %th Token
    %th Description
    %th Projects
    %th Builds Stats
    %th Registered
    %th

  = render @runners
= paginate @runners
