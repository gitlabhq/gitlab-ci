%h3
  Runner: #{@runner.token}

%p.light= @runner.description
%p
  = link_to runners_path do
    &larr; Back to runners list
%hr

.row-fluid
  .span3
    %h5 Projects assigned to this runner
    %ul
      - @runner.projects.each do |project|
        %li
          = link_to project.name, project

  .span9
    %h5 Recent builds served by this runner
    %table.builds
      %thead
        %tr
          %th Status
          %th Commit
          %th Message
          %th Branch
          %th Duration
          %th Finished at

      = render @builds
