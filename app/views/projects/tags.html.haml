= render 'shared/guide' if params[:show_guide].present?


%table.builds
  %thead
    %tr
      %th Status
      %th Commit
      %th Message
      %th Tag
      %th Duration
      %th Finished at

  = render @build_groups

= paginate @build_groups
- if @build_groups.empty?
  .bs-callout
    %h4 No tags yet

