%h1 Projects:
- @projects.each do |project|
  %div.alert{class: project_statuc_class(project) }
    .row
      .span6
        %h4
          #{project.name} : #{project.human_status}
      .span5
        %a.btn{href: project_path(project)} Builds
        %a.btn Run
        %a.btn{href: edit_project_path(project)}
          %i.icon-edit
          Edit

- if @projects.empty?
  .alert
    No projects yet
